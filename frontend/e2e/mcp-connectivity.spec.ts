import { test } from "@playwright/test";

// T20: wire this up against a running backend + frontend dev server.
test.skip("connects to MCP and shows jurisdictions", async () => {
  // Placeholder for end-to-end MCP connectivity coverage.
});
